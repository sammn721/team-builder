const { it, expect } = require("@jest/globals");
const { describe } = require("yargs");
const Intern = require("../lib/Intern");

describe("Intern", () => {
    describe("getSchool", () => {
        it("should do", () => {
            expect();
        })
    })
    describe("getRole", () => {
        it("should do", () => {
            expect();
        })
    })
});