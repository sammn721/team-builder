const { it, expect } = require("@jest/globals");
const { describe } = require("yargs");
const Engineer = require("../lib/Engineer");

describe("Engineer", () => {
    describe("getGithub", () => {
        it("should do", () => {
            expect();
        })
    })
    describe("getRole", () => {
        it("should do", () => {
            expect();
        })
    })
});